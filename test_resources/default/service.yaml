apiVersion: v1
kind: Service
metadata:
  name: go-agt-svc
spec:
  type: NodePort
  ports:
      # By default and for convenience, the `targetPort` is set to the same value as the `port` field.
    - protocol: TCP
      port: 80
      targetPort: 9000
